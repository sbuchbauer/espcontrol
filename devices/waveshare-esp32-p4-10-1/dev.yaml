# =============================================================================
# DEV CONFIG - Local development entry point (Waveshare 10.1" P4 panel)
# =============================================================================
# Flash with: esphome run dev.yaml
# =============================================================================

substitutions:
  name: "espcontrol-waveshare-10-1"
  friendly_name: "EspControl Waveshare 10.1"
  espcontrol_component_url: "../.."
  mdi_font_file: "../../common/assets/fonts/materialdesignicons-webfont-7.4.47.ttf"
  espcontrol_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

web_server:
  js_url: ""
  js_include: "../../docs/public/webserver/waveshare-esp32-p4-10-1/www.js"

packages:
  espcontrol: !include packages.yaml
